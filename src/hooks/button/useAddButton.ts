const useAddButton = () => {
  const props = {
    size: 'large',
    shape: 'circle',
    style: {
      position: 'fixed',
      bottom: '30px',
      right: '30px',
    },
  };
  return props;
};

export default useAddButton;
